name: ChatAutoMod
version: '1.0-SNAPSHOT'
main: org.originsrebirth.chatAutoMod.ChatAutoMod
api-version: '1.21'
commands:
  automodstring:
    description: "Gets the current regex master string"
    usage: "/automodstring"
  automodreload:
    description: "Reloads all config files, the recompiles the master string"
    usage: "/automodreload"
